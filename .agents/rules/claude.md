---
trigger: always_on
---

# Supplier Portal — Read CLAUDE.md First

Before performing ANY action in this repository, you MUST read and understand the root `CLAUDE.md` file.

This is mandatory.

## Required Order

Always follow this order:

1. Locate `CLAUDE.md` in the repository root.
2. Read it completely.
3. Read any additional instruction files referenced by `CLAUDE.md`, if relevant.
4. Inspect the repository and current implementation as required by the instructions.
5. Determine the correct approach for the user's request.
6. Only then execute commands, modify files, or implement changes.

## Important Rules

- Do NOT execute implementation commands before reading `CLAUDE.md`.
- Do NOT modify any file before reading `CLAUDE.md`.
- Do NOT rely on your generic coding knowledge when `CLAUDE.md` provides project-specific rules.
- Treat `CLAUDE.md` as mandatory project instructions, not optional documentation.
- Follow all applicable rules in `CLAUDE.md` throughout the task.
- If `CLAUDE.md` conflicts with the actual repository implementation, inspect the repository and clearly report the conflict before making a consequential change.
- Do not skip `CLAUDE.md` even for small tasks, bug fixes, UI changes, database changes, or documentation changes.

## Before Implementation

After reading `CLAUDE.md`, briefly confirm:

- `CLAUDE.md` was read.
- The relevant project rules were identified.
- The next inspection or implementation step is consistent with those rules.

Do not start implementation until this initialization step is complete.

## Core Instruction

READ `CLAUDE.md` FIRST.
UNDERSTAND IT.
FOLLOW IT.
THEN ACT.
# UI-04 Purchasing Result

Date: `2026-08-20`

## Status

`PASS` for code, compile, and functional regression coverage.

`VISUAL_QA_BLOCKED`: the in-app browser runtime was unavailable. No screenshot, responsive, keyboard, focus, or visual PASS is claimed.

## Scope completed

The remaining active Purchasing views were migrated onto the UI-01/UI-02 design foundation without changing their backend contracts:

- dashboard KPI, operational alerts, charts, recent PR, exchange-rate, and arrival surfaces;
- quotation index/detail, filters, AJAX replacement hooks, workflow actions, export, attachment, and conversion-rate presentation;
- inter-supplier, historical, and current-vs-best comparison views;
- purchase-order index/create/detail, consolidation, material summary, document tracking, PDF/export, arrival, QC, and claim context;
- material-claim index/create/detail;
- period management, reports, and conversation index.

New reusable presentation pieces:

- `resources/views/components/ui/metric-card.blade.php` for role dashboard/summary metrics;
- `resources/views/components/purchasing/comparison-tabs.blade.php` for the three comparison modes.

## Preserved behavior and compatibility

- DataTables remains the engine for server-side and client-side table behavior.
- Chart.js remains the engine for comparison and dashboard charts.
- SweetAlert/`AdasiAlert` confirmation and toast callsites remain active.
- Bootstrap modal, collapse, tab, tooltip, and remaining legacy callsites remain as a measured compatibility bridge.
- Existing form names, request methods, route names, Hashid route keys, DOM IDs, AJAX replacement IDs, data attributes, async export hooks, PDF confirmation hooks, and attachment URLs were preserved.
- No route, controller, model, migration, database schema/data, authorization, translation, Hashid, or workflow change was made.

## Verification

| Check | Result |
|---|---|
| `git diff --check` | PASS |
| `php artisan view:clear` + `php artisan view:cache` | PASS |
| `npm.cmd run build` | PASS — CSS 30.92 kB / 6.69 kB gzip; JS 95.72 kB / 34.99 kB gzip |
| Quotation/security batch | PASS — 15 tests, 233 assertions |
| Comparison/history batch | PASS — 11 tests, 200 assertions; 1 existing assertion-less test reported risky |
| PO/export/navigation batch | PASS — 27 tests, 370 assertions; 4 assertion-less tests reported risky |
| Claim/isolation batch | PASS — 20 tests, 180 assertions; 3 assertion-less tests reported risky |
| Full `php artisan test --compact` | 200 passed, 4 risky, 1 known pre-existing failure, 2179 assertions |

The only full-suite failure is the baseline `CustomAdasiAlertTest::test_export_confirmation_retains_the_single_download_guard`, which expects `window.exportConfirmationOpen` in `layouts/app.blade.php`. UI-04 did not modify that contract or the layout file.

## Static regression audit

Measured across the 15 migrated Purchasing views against checkpoint `39818d3`:

| Pattern | Before | After | Result |
|---|---:|---:|---|
| `<x-ui.*` usage | 0 | 158 | Shared component patterns adopted |
| legacy `class="card` openings | 163 | 45 | Reduced; remaining cards are compatibility-bound complex sections |
| inline `style=` | 81 | 51 | Reduced; remaining styles are chart/dynamic dimensions, image fitting, and compatibility details |
| `data-bs-*` | 37 | 35 | Retained where Bootstrap behavior is still active |
| `.DataTable(` | 5 | 5 | Engine/callsites preserved |
| `new Chart(` | 4 | 4 | Engine/callsites preserved |

- No literal hardcoded hexadecimal color was added to the migrated Purchasing views; the only hexadecimal-looking search hits are HTML entities (`&#039;`).
- Bootstrap Icons remains the only icon family.
- Guarded-path diff (`routes`, controllers, models, database) is empty.
- No database command or mutation was run.

## Design guidance applied

The previously loaded `design`, `ui-styling`, `ui-ux-pro-max`, and `design-system` guides were applied through the established token hierarchy, restrained elevation, consistent shapes, responsive grid patterns, explicit labels, state clarity, and reusable components. The ui-ux-pro-max helper remained unavailable (`GUIDE_HELPER_UNAVAILABLE`), so only the guide content actually loaded was used.

Material Design 3 and Coolors evidence remains recorded in UI-00/UI-01. No new MCP result was fabricated for this package.

## Remaining compatibility dependencies

- Bootstrap remains required for live modal/collapse/tab/tooltip callsites.
- jQuery remains required by DataTables and existing dynamic Purchasing flows.
- DataTables, Chart.js, and SweetAlert remain intentionally retained per fixed decisions.
- Complex historical-result HTML generated by JavaScript still emits compatibility classes and will be audited in UI-07.

## Next package

Proceed to `UI-05` Supplier using the proven shell, metric, form, data-table, state, and attachment patterns while preserving supplier data isolation.
